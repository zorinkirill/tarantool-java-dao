pluginManagement{
    plugins {
        id "io.github.gradle-nexus.publish-plugin" version "1.1.0"
    }
}

rootProject.name = "tarantool-java-dao"

include "tarantool-dao-api",
        "tarantool-dao-gen",
        "tarantool-dao-test"
